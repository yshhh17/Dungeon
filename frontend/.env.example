# API URL for connecting to the backend
# This is used by the browser (client-side), so it should be the URL 
# where your browser can reach the backend server.
# For Docker: use http://localhost:8000 (browser -> host -> container)
# For local development: use http://localhost:8000 or http://127.0.0.1:8000
VITE_API_URL=http://localhost:8000
